"""
sklearndoz — Clon de scikit-learn en Python puro (sin dependencias externas).

Módulos disponibles:
  linear_models     — LinearRegression, Ridge, Lasso, LogisticRegression, SGD*, ...
  tree              — DecisionTreeClassifier, DecisionTreeRegressor
  ensemble          — RandomForest*, GradientBoosting*, AdaBoost*, Bagging*
  neighbors         — KNeighbors*, RadiusNeighbors*, NearestCentroid, LocalOutlierFactor
  naive_bayes       — GaussianNB, MultinomialNB, BernoulliNB, ComplementNB, CategoricalNB
  svm               — SVC, LinearSVC, NuSVC, SVR, LinearSVR, OneClassSVM
  cluster           — KMeans, MiniBatchKMeans, DBSCAN, AgglomerativeClustering, MeanShift, GaussianMixture
  decomposition     — PCA, TruncatedSVD, NMF, FastICA, FactorAnalysis, LinearDiscriminantAnalysis
  preprocessing     — StandardScaler, MinMaxScaler, RobustScaler, LabelEncoder, OneHotEncoder, ...
  model_selection   — train_test_split, KFold, StratifiedKFold, cross_val_score, GridSearchCV, ...
  neural_network    — MLPClassifier, MLPRegressor
  pipeline          — Pipeline, FeatureUnion, ColumnTransformer, make_pipeline
  feature_selection — SelectKBest, VarianceThreshold, RFE, SelectFromModel
  manifold          — TSNE, MDS, Isomap
  multiclass        — OneVsRestClassifier, OneVsOneClassifier, OutputCodeClassifier
  dummy             — DummyClassifier, DummyRegressor
  isotonic          — IsotonicRegression
  calibration       — CalibratedClassifierCV
"""
